message Announce {
  required bytes id = 1;
  required uint32 hops = 2;
  optional bytes type = 3;
  optional bytes data = 4;
  optional uint32 limit = 5;
}

message Message {
  oneof msg {
    Announce request = 1;
    Announce response = 2;
  }
}
