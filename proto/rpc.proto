message Announce {
  required bytes id = 1;
  optional bytes type = 2;
  optional bytes data = 3;
  optional uint32 hops = 4;
  optional uint32 limit = 5;
}

message Message {
  oneof msg {
    Announce request = 1;
    Announce response = 2;
  }
}
